rules:
  - id: find-inline-html-js-events
    languages: [html]
    severity: WARNING
    message: "發現 HTML 內聯事件屬性 ($ATTR=$VALUE)。"
    patterns:
      - pattern: <$TAG $ATTR="...">
      - metavariable-regex:
          metavariable: "$ATTR"
          regex: '(?i)^on.*'